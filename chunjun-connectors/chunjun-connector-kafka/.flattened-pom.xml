<?xml version="1.0" encoding="UTF-8"?>
<project xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd" xmlns="http://maven.apache.org/POM/4.0.0"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance">
  <modelVersion>4.0.0</modelVersion>
  <parent>
    <groupId>com.dtstack.chunjun</groupId>
    <artifactId>chunjun-connectors</artifactId>
    <version>1.12.5-4.1.0-SNAPSHOT</version>
  </parent>
  <groupId>com.dtstack.chunjun</groupId>
  <artifactId>chunjun-connector-kafka</artifactId>
  <version>1.12.5-4.1.0-SNAPSHOT</version>
  <name>ChunJun : Connectors : Kafka</name>
  <licenses>
    <license>
      <name>GNU Affero General Public License</name>
      <url>https://www.gnu.org/licenses/agpl-3.0.en.html</url>
      <distribution>repo</distribution>
    </license>
  </licenses>
  <properties>
    <flink.connector-kafka.version>3.0.2-1.18</flink.connector-kafka.version>
    <jackson.version>2.8.0</jackson.version>
    <kafka.version>2.8.0</kafka.version>
  </properties>
  <dependencies>
    <dependency>
      <groupId>org.apache.flink</groupId>
      <artifactId>flink-connector-kafka</artifactId>
      <version>${flink.connector-kafka.version}</version>
      <exclusions>
        <exclusion>
          <groupId>org.apache.flink</groupId>
          <artifactId>flink-core</artifactId>
        </exclusion>
      </exclusions>
    </dependency>
    <dependency>
      <groupId>com.alibaba</groupId>
      <artifactId>fastjson</artifactId>
      <version>1.2.83</version>
    </dependency>
    <dependency>
      <groupId>org.apache.flink</groupId>
      <artifactId>flink-avro-confluent-registry</artifactId>
      <version>${flink.version}</version>
    </dependency>
    <dependency>
      <groupId>org.apache.avro</groupId>
      <artifactId>avro</artifactId>
      <version>1.10.0</version>
    </dependency>
  </dependencies>
  <build>
    <plugins>
      <plugin>
        <artifactId>maven-shade-plugin</artifactId>
        <version>3.2.4</version>
        <executions>
          <execution>
            <phase>none</phase>
            <goals>
              <goal>shade</goal>
            </goals>
            <configuration>
              <createDependencyReducedPom>false</createDependencyReducedPom>
              <relocations>
                <relocation>
                  <pattern>com.alibaba.fastjson</pattern>
                  <shadedPattern>com.shade.alibaba.fastjson</shadedPattern>
                </relocation>
                <relocation>
                  <pattern>org.apache.avro</pattern>
                  <shadedPattern>org.apache.flink.avro.shaded.org.apache.avro</shadedPattern>
                </relocation>
                <relocation>
                  <pattern>com.fasterxml.jackson</pattern>
                  <shadedPattern>org.apache.flink.avro.shaded.com.fasterxml.jackson</shadedPattern>
                </relocation>
                <relocation>
                  <pattern>org.apache.commons.compress</pattern>
                  <shadedPattern>org.apache.flink.avro.shaded.org.apache.commons.compress</shadedPattern>
                </relocation>
                <relocation>
                  <pattern>com.google.protobuf</pattern>
                  <shadedPattern>com.dtstack.chunjun.connector.kafka.shaded.com.google.protobuf</shadedPattern>
                </relocation>
              </relocations>
              <artifactSet>
                <excludes>
                  <exclude>org.slf4j</exclude>
                </excludes>
              </artifactSet>
              <filters>
                <filter>
                  <artifact>org.apache.kafka:*</artifact>
                  <excludes>
                    <exclude>kafka/kafka-version.properties</exclude>
                    <exclude>LICENSE</exclude>
                    <exclude>NOTICE</exclude>
                    <exclude>common/**</exclude>
                  </excludes>
                </filter>
                <filter>
                  <artifact>*:*</artifact>
                  <excludes>
                    <exclude>META-INF/*.SF</exclude>
                    <exclude>META-INF/*.DSA</exclude>
                    <exclude>META-INF/*.RSA</exclude>
                  </excludes>
                </filter>
                <filter>
                  <artifact>org.apache.flink:flink-avro</artifact>
                  <excludes>
                    <exclude>META-INF/services/*</exclude>
                  </excludes>
                </filter>
                <filter>
                  <artifact>org.apache.flink:flink-connector-kafka</artifact>
                  <excludes>
                    <exclude>META-INF/services/*</exclude>
                  </excludes>
                </filter>
                <filter>
                  <artifact>org.glassfish.jersey.core:jersey-common</artifact>
                  <excludes>
                    <exclude>META-INF/services/*</exclude>
                  </excludes>
                </filter>
              </filters>
              <transformers>
                <transformer implementation="org.apache.maven.plugins.shade.resource.ServicesResourceTransformer" />
              </transformers>
            </configuration>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <artifactId>maven-antrun-plugin</artifactId>
        <executions>
          <execution>
            <id>copy-resources</id>
            <phase>none</phase>
            <goals>
              <goal>run</goal>
            </goals>
            <configuration>
              <tasks>
                <copy file="${basedir}/target/${project.artifactId}-${project.version}.jar" todir="${basedir}/../../${dist.dir}/connector/kafka" />
                <move file="${basedir}/../../${dist.dir}/connector/kafka/${project.artifactId}-${project.version}.jar" tofile="${basedir}/../../${dist.dir}/connector/kafka/${project.artifactId}-${git.branch}.jar" />
                <delete>
                  <fileset excludes="${project.artifactId}-${git.branch}.jar" includes="${project.artifactId}-*.jar" dir="${basedir}/../../${dist.dir}/connector/kafka/" />
                </delete>
                <copy file="${basedir}/target/${project.artifactId}-${project.version}.jar" todir="${basedir}/../../${dist.dir}/connector/upsert-kafka" />
                <move file="${basedir}/../../${dist.dir}/connector/upsert-kafka/${project.artifactId}-${project.version}.jar" tofile="${basedir}/../../${dist.dir}/connector/upsert-kafka/${project.artifactId}-${git.branch}.jar" />
                <delete>
                  <fileset excludes="${project.artifactId}-${git.branch}.jar" includes="${project.artifactId}-*.jar" dir="${basedir}/../../${dist.dir}/connector/upsert-kafka/" />
                </delete>
              </tasks>
            </configuration>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>
</project>
